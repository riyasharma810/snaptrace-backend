import randomdef analyze_file(filepath):    # ⚠️ Abhi dummy logic hai (random result)    # Later yahan tumhara trained model aayega        result = random.choice(["Real", "Fake"])    confidence = round(random.uniform(75, 99), 2)    detected_model = random.choice([        "StyleGAN",        "MidJourney",        "Stable Diffusion",        "Unknown"    ])    return {        "result": result,        "confidence": confidence,        "model": detected_model    }